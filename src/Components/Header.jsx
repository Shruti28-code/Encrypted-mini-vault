import React from "react";

export default function Header({ name }) {
    return (
        <header className="flex items-center justify-between mb-8">

            {/* You can add top nav controls here if needed */}
        </header>
    );
}
